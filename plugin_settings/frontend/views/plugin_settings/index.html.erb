<%= form_for @plugin_setting, :as => "plugin_setting", :url => {:action => :create}, :html => {:class => 'form-horizontal aspace-record-form'} do |f| %>

  <%= form_context :plugin_setting, @plugin_setting do |form| %>

<div class="row-fluid">
   <div class="span3">
   </div>
   <div class="span9">
     <div class="record-pane">
       <h2><%= I18n.t("plugins.plugin_settings.title") %></h2>
    </div>
     <div class="row-fluid">
       <div class="span12">
         <%= render_aspace_partial :partial => "plugin_settings/form", :locals => {:form => form} %>
       </div>
     </div>
  </div>
</div>

  <% end %>
<% end %>